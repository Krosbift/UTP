from app.main import main


if __name__ == "__main__":
  """
  This script serves as the entry point for the Images Processor application.
  It imports the main function from the app.main module and executes it when the script is run directly.
  Usage:
    python __main__.py
  Modules:
    app.main: Contains the main function that initializes and runs the application.
  Functions:
    main(): The main function that starts the application.
  """
  main()

